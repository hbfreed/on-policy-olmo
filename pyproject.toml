[project]
name = "on-policy-olmo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "liger-kernel>=0.6.4",
    "torch==2.9.1",
    "torchao>=0.15.0",
    "transformers>=5.0.0",
]
